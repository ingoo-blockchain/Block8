import styled from "styled-components"

export const Nav = styled.div`
    ul {
        display: flex;
    }

    li {
        margin-left: 10px;
    }

    a {
        color: #fff;
        padding: 5px;

        &:hover {
            color: #eee;
        }
    }
`
